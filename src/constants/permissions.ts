// TODO: Scaffolded for route-level access control constants (e.g., ROUTE_ACCESS map).
// Currently unused — role checking is handled via route `meta.requiresAuth` and the
// navigation guard in router/index.ts. Populate this file when per-route role
// whitelisting is required (e.g., meta: { roles: ['admin', 'coordinator'] }).
export {}
