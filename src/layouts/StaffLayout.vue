<!-- TODO: Scaffolded for the staff/admin-facing app layout wrapper.
     Implement when staff views are migrated away from the legacy AppShell.vue.

     Planned: identical chrome to MemberLayout.vue but with a different sidebar
     nav set (admin routes). Consider consolidating both into a single AppLayout.vue
     that reads nav items from route meta or the auth store role. -->
