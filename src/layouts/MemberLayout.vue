<!-- TODO: Scaffolded for the member-facing app layout wrapper.
     Implement when DashboardView.vue and NewEntryView.vue are migrated away from
     the legacy AppShell.vue.

     Planned: wrap Sidebar.vue + main content slot, resolve nav items from route meta.
     Consider consolidating with StaffLayout.vue into a single AppLayout.vue that
     accepts a `role` prop or reads from the auth store to customise the sidebar. -->
